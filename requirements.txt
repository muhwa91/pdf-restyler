PyMuPDF>=1.24
PySide6>=6.6
pyqtdarktheme-fork>=2.3   # 원본 pyqtdarktheme 2.x는 PyPI에서 yanked → 유지보수 포크(동일 qdarktheme 모듈)

# 빌드/리소스 도구 (실행에는 불필요)
Pillow>=10.0      # svg→ico 멀티해상도 변환
pyinstaller>=6.0  # 단독 실행 exe 빌드
